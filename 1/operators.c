#include <stdio.h>

int main() {
	// Binary operators
	int a = 3 * 5;
	float b = 5 * 0.5;
	float c = a + b;
	float remainder = 10 % 2;
	float div = 10 / 2;

	// Unary operators
	int pos = +10;
	int negative = -pos;

	return 0;
}